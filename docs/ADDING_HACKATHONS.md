# Adding Hackathons

1. Create the hackathon directory under `data/`.
2. Add `hackathon.json`.
3. Add one or more edition directories.
4. Add `edition.json` to each edition.
5. Add problem Markdown files under `problems/`.
6. Run `npm run check`.

No application source changes are required.
